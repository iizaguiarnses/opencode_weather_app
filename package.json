{
  "name": "02-weather",
  "module": "index.ts",
  "type": "module",
  "private": true,
    "scripts": {
    "build": "bun test && bun build --compile index.ts --outfile weather",
    "start": "bun run index.ts",
    "dev": "bun run src/index.ts --watch",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "tsc": "bunx tsc"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  }
}
